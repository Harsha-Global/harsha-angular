import { TruncateImpurePipe } from './truncate-impure-pipe';

describe('TruncateImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
